import React from 'react';

const Header = (props) => {
    return (
        <div className={props.toggleMode ? "header" : "darkheader"}>
           
        </div>
    );
}

export default Header;